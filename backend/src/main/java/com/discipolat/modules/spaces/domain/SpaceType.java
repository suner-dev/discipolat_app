package com.discipolat.modules.spaces.domain;

/**
 * G2.6 — Un espace est un objet unifié : un DÉPARTEMENT, une FAMILLE et une
 * SOUS-ÉQUIPE partagent exactement le même modèle, seuls les droits diffèrent.
 */
public enum SpaceType {
    DEPARTMENT,
    FAMILY,
    SUB_TEAM
}

package com.discipolat.modules.spaces.domain;

/** G2.6 — Cycle de vie d'un espace (jamais un enum métier figé : pur cycle technique). */
public enum SpaceStatus {
    ACTIVE,
    INACTIVE,
    ARCHIVED
}

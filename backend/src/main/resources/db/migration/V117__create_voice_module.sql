-- V117 — Module vocal (tables déjà créées par migrations antérieures V93+).
-- Fichier conservé vide (idempotent) pour garder la continuité de la
-- numérotation Flyway sans rejouer de DDL.
SELECT 1;
